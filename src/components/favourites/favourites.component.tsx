import { FC } from 'react';

import { Grid } from 'ui/grid/grid.component';

export const Favourites: FC = () => {
  return (
    <Grid>
      <div>
        <h1>Favourites</h1>
      </div>
    </Grid>
  );
};
